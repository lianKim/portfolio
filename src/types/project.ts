export interface Project {
  slug: string
  label: string
  title: string
  description: string
  order: number
}

export interface ProjectFrontmatter {
  label: string
  title: string
  description: string
  order: number
}
